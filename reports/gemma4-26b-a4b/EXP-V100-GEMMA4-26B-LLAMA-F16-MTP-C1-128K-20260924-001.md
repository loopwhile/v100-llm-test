# EXP-V100-GEMMA4-26B-LLAMA-F16-MTP-C1-128K-20260924-001

## Result

- Verdict: **FAIL_STARTUP**
- Model: Gemma4-26B-A4B-IT-QAT
- Weight: UD-Q4_K_XL
- KV: FP16
- Runtime: llama.cpp 10775 / 67a17c17caa95742186f8b1ecadd1b5abd6d5ebb
- Topology: TP2 shared
- Speculation: explicit Gemma4 assistant MTP
- Draft depth: 4
- Draft device: CUDA0

## Artifact identity

Both pinned artifacts passed SHA256 verification before server launch.

- target: `a7c5bc715f5ff8e99a3e8901ce7d2b42b402c669bf24f7c5250747633d0f5891`
- MTP companion: `7272d97595f0d4c74bd7b623492b7dbdaafd8b7c72f329a8270ba4eca68f768a`

The runtime preflight also passed the draft-mtp, draft-model, draft-device and companion-path checks.

## Failure evidence

No measured request was issued. The server exited during startup with process code **139**.

Relevant sequence:

1. memory fitting emitted `Gemma4Assistant requires ctx_other to be set` and explicitly described it as a warning normal during memory fitting;
2. the runtime proceeded to `loading draft model '/model/draft.gguf'`;
3. a native backtrace followed through `common_speculative_init_result` / `common_speculative_init_from_params`;
4. the runner recorded `FAIL_STARTUP`.

This is not classified as OOM and not as an artifact checksum failure.

Subsequent single-variable diagnostics isolated this attempt as a **CUDA0-only draft-device placement failure** on the pinned b10775 + 2×V100 TP2 layer-split topology. Changing only `--spec-draft-device CUDA0` to `CUDA0,CUDA1` produced startup PASS, and corrected measured attempt `...-002` completed C1 128K successfully. The upstream issue comparison is therefore no longer the primary classification.

## Superseded disposition

This attempt is retained as immutable evidence for the CUDA0-only drafter configuration. It is **not** the final Gemma4 MTP lane verdict. Corrected attempt `EXP-V100-GEMMA4-26B-LLAMA-F16-MTP-C1-128K-20260924-002` uses `CUDA0,CUDA1` and passes C1 128K. MTP_NGRAM is therefore reopened under the corrected dual-device contract.

## Evidence

- Raw: `results/raw/EXP-V100-GEMMA4-26B-LLAMA-F16-MTP-C1-128K-20260924-001`
- Upstream reference: https://github.com/ggml-org/llama.cpp/issues/25828
